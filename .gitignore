# secrets — NEVER commit
.env
.env.local
.env.*.local

# node / next
node_modules/
.next/
dist/

# foundry
cache/
out/
broadcast/

# sqlite recipe store — never commit the DB
data/
*.db
*.db-wal
*.db-shm

# misc
.DS_Store
*.log

# tooling / local (scheduler + workflow scratch, build caches)
.claude/
.workflow/
.turbo/
*.tsbuildinfo
.pnpm-store/
